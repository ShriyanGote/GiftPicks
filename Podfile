# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'  # Adjust the version as necessary for your project

target 'GiftPicks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GiftPicks
  pod 'AWSS3'

  # If you have a tests target and it's correctly named in your Xcode project, uncomment these lines
  # target 'GiftPicksTests' do
  #   inherit! :search_paths
  #   # Pods for testing
  # end

  # Similarly for UITests, ensure the target exists or remove these lines
  # target 'GiftPicksUITests' do
  #   # Pods for UITests
  # end

end
